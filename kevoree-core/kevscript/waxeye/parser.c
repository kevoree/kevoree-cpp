/*
 * Generated by the Waxeye Parser Generator - version 0.8.0
 * www.waxeye.org
 */

#define PARSER_C_
#include "parser.h"
#include <assert.h>

const char *type_strings[] = {
    "KevScript",
    "Statement",
    "Add",
    "Remove",
    "Move",
    "Attach",
    "Detach",
    "Set",
    "Network",
    "AddBinding",
    "DelBinding",
    "AddRepo",
    "Include",
    "NameList",
    "TypeDef",
    "Namespace",
    "Start",
    "Stop",
    "Pause",
    "InstancePath",
    "Wildcard",
    "String",
    "String2",
    "Version",
    "Line",
    "RealString",
    "Escaped",
    "SingleQuoteLine",
    "DoubleQuoteLine",
    "RealStringNoNewLine",
    "NewLine",
    "RepoToken",
    "IncludeToken",
    "AddToken",
    "RemoveToken",
    "MoveToken",
    "SetToken",
    "AttachToken",
    "DetachToken",
    "NetworkToken",
    "BindToken",
    "UnbindToken",
    "NamespaceToken",
    "StartToken",
    "StopToken",
    "PauseToken",
    "Comment",
    "eol",
    "ws"
};

struct parser_t* parser_new() {
    const size_t start = 0;
    const bool eof_check = true;
    size_t num_edges;
    size_t num_states;
    const size_t num_automata = 72;
    struct trans_t trans;
    union trans_data trans_d;
    struct edge_t *edges;
    struct state_t *states;
    struct fa_t *automata = calloc(num_automata, sizeof(struct fa_t));
    assert(automata != NULL);

    num_states = 4;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 48;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 1, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 2;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 1;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 2, false);
    trans_d.fa = 46;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[1], trans, 3, true);
    state_init(&states[1], edges, num_edges, true);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 48;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 1, false);
    state_init(&states[2], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 47;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 2, false);
    state_init(&states[3], edges, num_edges, true);
    fa_init(&automata[0], MODE_LEFT, TYPE_KEVSCRIPT, states, num_states);

    num_states = 2;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 15;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 2;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 1, false);
    trans_d.fa = 3;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[1], trans, 1, false);
    trans_d.fa = 4;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[2], trans, 1, false);
    trans_d.fa = 5;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[3], trans, 1, false);
    trans_d.fa = 6;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[4], trans, 1, false);
    trans_d.fa = 7;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[5], trans, 1, false);
    trans_d.fa = 9;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[6], trans, 1, false);
    trans_d.fa = 10;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[7], trans, 1, false);
    trans_d.fa = 12;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[8], trans, 1, false);
    trans_d.fa = 8;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[9], trans, 1, false);
    trans_d.fa = 11;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[10], trans, 1, false);
    trans_d.fa = 15;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[11], trans, 1, false);
    trans_d.fa = 16;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[12], trans, 1, false);
    trans_d.fa = 17;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[13], trans, 1, false);
    trans_d.fa = 18;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[14], trans, 1, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 0;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    state_init(&states[1], edges, num_edges, true);
    fa_init(&automata[1], MODE_LEFT, TYPE_STATEMENT, states, num_states);

    num_states = 8;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 33;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 1, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 48;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 2, false);
    state_init(&states[1], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 13;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 3, false);
    state_init(&states[2], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 48;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 4, false);
    state_init(&states[3], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = ':';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 5, true);
    state_init(&states[4], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 48;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 6, false);
    state_init(&states[5], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 14;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 7, false);
    state_init(&states[6], edges, num_edges, false);
    num_edges = 0;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    state_init(&states[7], edges, num_edges, true);
    fa_init(&automata[2], MODE_LEFT, TYPE_ADD, states, num_states);

    num_states = 4;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 34;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 1, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 48;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 2, false);
    state_init(&states[1], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 13;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 3, false);
    state_init(&states[2], edges, num_edges, false);
    num_edges = 0;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    state_init(&states[3], edges, num_edges, true);
    fa_init(&automata[3], MODE_LEFT, TYPE_REMOVE, states, num_states);

    num_states = 6;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 35;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 1, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 48;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 2, false);
    state_init(&states[1], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 13;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 3, false);
    state_init(&states[2], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 48;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 4, false);
    state_init(&states[3], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 19;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 5, false);
    state_init(&states[4], edges, num_edges, false);
    num_edges = 0;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    state_init(&states[5], edges, num_edges, true);
    fa_init(&automata[4], MODE_LEFT, TYPE_MOVE, states, num_states);

    num_states = 6;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 37;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 1, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 48;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 2, false);
    state_init(&states[1], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 13;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 3, false);
    state_init(&states[2], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 48;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 4, false);
    state_init(&states[3], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 19;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 5, false);
    state_init(&states[4], edges, num_edges, false);
    num_edges = 0;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    state_init(&states[5], edges, num_edges, true);
    fa_init(&automata[5], MODE_LEFT, TYPE_ATTACH, states, num_states);

    num_states = 6;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 38;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 1, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 48;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 2, false);
    state_init(&states[1], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 13;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 3, false);
    state_init(&states[2], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 48;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 4, false);
    state_init(&states[3], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 19;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 5, false);
    state_init(&states[4], edges, num_edges, false);
    num_edges = 0;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    state_init(&states[5], edges, num_edges, true);
    fa_init(&automata[6], MODE_LEFT, TYPE_DETACH, states, num_states);

    num_states = 10;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 36;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 1, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 48;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 2, false);
    state_init(&states[1], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 19;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 3, false);
    state_init(&states[2], edges, num_edges, false);
    num_edges = 2;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = '/';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 4, true);
    trans_d.fa = 48;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[1], trans, 6, false);
    state_init(&states[3], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 19;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 5, false);
    state_init(&states[4], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 48;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 6, false);
    state_init(&states[5], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = '=';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 7, true);
    state_init(&states[6], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 48;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 8, false);
    state_init(&states[7], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 25;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 9, false);
    state_init(&states[8], edges, num_edges, false);
    num_edges = 0;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    state_init(&states[9], edges, num_edges, true);
    fa_init(&automata[7], MODE_LEFT, TYPE_SET, states, num_states);

    num_states = 6;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 39;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 1, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 48;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 2, false);
    state_init(&states[1], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 19;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 3, false);
    state_init(&states[2], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 48;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 4, false);
    state_init(&states[3], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 22;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 5, false);
    state_init(&states[4], edges, num_edges, false);
    num_edges = 0;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    state_init(&states[5], edges, num_edges, true);
    fa_init(&automata[8], MODE_LEFT, TYPE_NETWORK, states, num_states);

    num_states = 6;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 40;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 1, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 48;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 2, false);
    state_init(&states[1], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 19;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 3, false);
    state_init(&states[2], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 48;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 4, false);
    state_init(&states[3], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 19;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 5, false);
    state_init(&states[4], edges, num_edges, false);
    num_edges = 0;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    state_init(&states[5], edges, num_edges, true);
    fa_init(&automata[9], MODE_LEFT, TYPE_ADDBINDING, states, num_states);

    num_states = 6;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 41;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 1, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 48;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 2, false);
    state_init(&states[1], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 19;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 3, false);
    state_init(&states[2], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 48;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 4, false);
    state_init(&states[3], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 19;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 5, false);
    state_init(&states[4], edges, num_edges, false);
    num_edges = 0;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    state_init(&states[5], edges, num_edges, true);
    fa_init(&automata[10], MODE_LEFT, TYPE_DELBINDING, states, num_states);

    num_states = 4;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 31;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 1, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 48;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 2, false);
    state_init(&states[1], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 29;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 3, false);
    state_init(&states[2], edges, num_edges, false);
    num_edges = 0;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    state_init(&states[3], edges, num_edges, true);
    fa_init(&automata[11], MODE_LEFT, TYPE_ADDREPO, states, num_states);

    num_states = 6;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 32;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 1, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 48;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 2, false);
    state_init(&states[1], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 21;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 3, false);
    state_init(&states[2], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = ':';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 4, true);
    state_init(&states[3], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 22;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 5, false);
    state_init(&states[4], edges, num_edges, false);
    num_edges = 0;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    state_init(&states[5], edges, num_edges, true);
    fa_init(&automata[12], MODE_LEFT, TYPE_INCLUDE, states, num_states);

    num_states = 5;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 19;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 1, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 48;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 2, false);
    state_init(&states[1], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    char *single;
    char *min;
    char *max;
    size_t num_single;
    size_t num_range;
    num_single = 1;
    num_range = 0;
    single = calloc(num_single, sizeof(char));
    assert(single != NULL);
    single[0] = ',';
    min = calloc(num_range, sizeof(char));
    assert(min != NULL);
    max = calloc(num_range, sizeof(char));
    assert(max != NULL);
    trans_d.set = set_new(single, num_single, min, max, num_range);
    trans_init(&trans, TRANS_SET, trans_d);
    edge_init(&edges[0], trans, 3, true);
    state_init(&states[2], edges, num_edges, true);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 48;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 4, false);
    state_init(&states[3], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 19;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 2, false);
    state_init(&states[4], edges, num_edges, false);
    fa_init(&automata[13], MODE_LEFT, TYPE_NAMELIST, states, num_states);

    num_states = 4;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 21;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 1, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = '/';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 2, true);
    state_init(&states[1], edges, num_edges, true);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 23;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 3, false);
    state_init(&states[2], edges, num_edges, false);
    num_edges = 0;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    state_init(&states[3], edges, num_edges, true);
    fa_init(&automata[14], MODE_LEFT, TYPE_TYPEDEF, states, num_states);

    num_states = 4;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 42;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 1, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 48;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 2, false);
    state_init(&states[1], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 21;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 3, false);
    state_init(&states[2], edges, num_edges, false);
    num_edges = 0;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    state_init(&states[3], edges, num_edges, true);
    fa_init(&automata[15], MODE_LEFT, TYPE_NAMESPACE, states, num_states);

    num_states = 4;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 43;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 1, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 48;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 2, false);
    state_init(&states[1], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 13;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 3, false);
    state_init(&states[2], edges, num_edges, false);
    num_edges = 0;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    state_init(&states[3], edges, num_edges, true);
    fa_init(&automata[16], MODE_LEFT, TYPE_START, states, num_states);

    num_states = 4;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 44;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 1, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 48;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 2, false);
    state_init(&states[1], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 13;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 3, false);
    state_init(&states[2], edges, num_edges, false);
    num_edges = 0;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    state_init(&states[3], edges, num_edges, true);
    fa_init(&automata[17], MODE_LEFT, TYPE_STOP, states, num_states);

    num_states = 4;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 45;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 1, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 48;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 2, false);
    state_init(&states[1], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 13;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 3, false);
    state_init(&states[2], edges, num_edges, false);
    num_edges = 0;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    state_init(&states[3], edges, num_edges, true);
    fa_init(&automata[18], MODE_LEFT, TYPE_PAUSE, states, num_states);

    num_states = 3;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 2;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 20;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 1, false);
    trans_d.fa = 21;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[1], trans, 1, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    num_single = 1;
    num_range = 0;
    single = calloc(num_single, sizeof(char));
    assert(single != NULL);
    single[0] = '.';
    min = calloc(num_range, sizeof(char));
    assert(min != NULL);
    max = calloc(num_range, sizeof(char));
    assert(max != NULL);
    trans_d.set = set_new(single, num_single, min, max, num_range);
    trans_init(&trans, TRANS_SET, trans_d);
    edge_init(&edges[0], trans, 2, true);
    state_init(&states[1], edges, num_edges, true);
    num_edges = 2;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 20;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 1, false);
    trans_d.fa = 21;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[1], trans, 1, false);
    state_init(&states[2], edges, num_edges, false);
    fa_init(&automata[19], MODE_LEFT, TYPE_INSTANCEPATH, states, num_states);

    num_states = 2;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = '*';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 1, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 0;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    state_init(&states[1], edges, num_edges, true);
    fa_init(&automata[20], MODE_LEFT, TYPE_WILDCARD, states, num_states);

    num_states = 2;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    num_single = 2;
    num_range = 3;
    single = calloc(num_single, sizeof(char));
    assert(single != NULL);
    single[0] = '-';
    single[1] = '_';
    min = calloc(num_range, sizeof(char));
    assert(min != NULL);
    min[0] = '0';
    min[1] = 'A';
    min[2] = 'a';
    max = calloc(num_range, sizeof(char));
    assert(max != NULL);
    max[0] = '9';
    max[1] = 'Z';
    max[2] = 'z';
    trans_d.set = set_new(single, num_single, min, max, num_range);
    trans_init(&trans, TRANS_SET, trans_d);
    edge_init(&edges[0], trans, 1, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    num_single = 2;
    num_range = 3;
    single = calloc(num_single, sizeof(char));
    assert(single != NULL);
    single[0] = '-';
    single[1] = '_';
    min = calloc(num_range, sizeof(char));
    assert(min != NULL);
    min[0] = '0';
    min[1] = 'A';
    min[2] = 'a';
    max = calloc(num_range, sizeof(char));
    assert(max != NULL);
    max[0] = '9';
    max[1] = 'Z';
    max[2] = 'z';
    trans_d.set = set_new(single, num_single, min, max, num_range);
    trans_init(&trans, TRANS_SET, trans_d);
    edge_init(&edges[0], trans, 1, false);
    state_init(&states[1], edges, num_edges, true);
    fa_init(&automata[21], MODE_LEFT, TYPE_STRING, states, num_states);

    num_states = 2;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    num_single = 2;
    num_range = 4;
    single = calloc(num_single, sizeof(char));
    assert(single != NULL);
    single[0] = '%';
    single[1] = '_';
    min = calloc(num_range, sizeof(char));
    assert(min != NULL);
    min[0] = '-';
    min[1] = '0';
    min[2] = '@';
    min[3] = 'a';
    max = calloc(num_range, sizeof(char));
    assert(max != NULL);
    max[0] = '.';
    max[1] = ':';
    max[2] = 'Z';
    max[3] = 'z';
    trans_d.set = set_new(single, num_single, min, max, num_range);
    trans_init(&trans, TRANS_SET, trans_d);
    edge_init(&edges[0], trans, 1, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    num_single = 2;
    num_range = 4;
    single = calloc(num_single, sizeof(char));
    assert(single != NULL);
    single[0] = '%';
    single[1] = '_';
    min = calloc(num_range, sizeof(char));
    assert(min != NULL);
    min[0] = '-';
    min[1] = '0';
    min[2] = '@';
    min[3] = 'a';
    max = calloc(num_range, sizeof(char));
    assert(max != NULL);
    max[0] = '.';
    max[1] = ':';
    max[2] = 'Z';
    max[3] = 'z';
    trans_d.set = set_new(single, num_single, min, max, num_range);
    trans_init(&trans, TRANS_SET, trans_d);
    edge_init(&edges[0], trans, 1, false);
    state_init(&states[1], edges, num_edges, true);
    fa_init(&automata[22], MODE_LEFT, TYPE_STRING2, states, num_states);

    num_states = 2;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    num_single = 1;
    num_range = 4;
    single = calloc(num_single, sizeof(char));
    assert(single != NULL);
    single[0] = '_';
    min = calloc(num_range, sizeof(char));
    assert(min != NULL);
    min[0] = '-';
    min[1] = '0';
    min[2] = 'A';
    min[3] = 'a';
    max = calloc(num_range, sizeof(char));
    assert(max != NULL);
    max[0] = '.';
    max[1] = '9';
    max[2] = 'Z';
    max[3] = 'z';
    trans_d.set = set_new(single, num_single, min, max, num_range);
    trans_init(&trans, TRANS_SET, trans_d);
    edge_init(&edges[0], trans, 1, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    num_single = 1;
    num_range = 4;
    single = calloc(num_single, sizeof(char));
    assert(single != NULL);
    single[0] = '_';
    min = calloc(num_range, sizeof(char));
    assert(min != NULL);
    min[0] = '-';
    min[1] = '0';
    min[2] = 'A';
    min[3] = 'a';
    max = calloc(num_range, sizeof(char));
    assert(max != NULL);
    max[0] = '.';
    max[1] = '9';
    max[2] = 'Z';
    max[3] = 'z';
    trans_d.set = set_new(single, num_single, min, max, num_range);
    trans_init(&trans, TRANS_SET, trans_d);
    edge_init(&edges[0], trans, 1, false);
    state_init(&states[1], edges, num_edges, true);
    fa_init(&automata[23], MODE_LEFT, TYPE_VERSION, states, num_states);

    num_states = 4;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 50;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 1, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = '\0';
    trans_init(&trans, TRANS_WILD, trans_d);
    edge_init(&edges[0], trans, 2, false);
    state_init(&states[1], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 49;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 3, false);
    state_init(&states[2], edges, num_edges, true);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = '\0';
    trans_init(&trans, TRANS_WILD, trans_d);
    edge_init(&edges[0], trans, 2, false);
    state_init(&states[3], edges, num_edges, false);
    fa_init(&automata[24], MODE_LEFT, TYPE_LINE, states, num_states);

    num_states = 4;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 2;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    num_single = 1;
    num_range = 0;
    single = calloc(num_single, sizeof(char));
    assert(single != NULL);
    single[0] = '\'';
    min = calloc(num_range, sizeof(char));
    assert(min != NULL);
    max = calloc(num_range, sizeof(char));
    assert(max != NULL);
    trans_d.set = set_new(single, num_single, min, max, num_range);
    trans_init(&trans, TRANS_SET, trans_d);
    edge_init(&edges[0], trans, 1, true);
    num_single = 1;
    num_range = 0;
    single = calloc(num_single, sizeof(char));
    assert(single != NULL);
    single[0] = '"';
    min = calloc(num_range, sizeof(char));
    assert(min != NULL);
    max = calloc(num_range, sizeof(char));
    assert(max != NULL);
    trans_d.set = set_new(single, num_single, min, max, num_range);
    trans_init(&trans, TRANS_SET, trans_d);
    edge_init(&edges[1], trans, 3, true);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 4;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 30;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 1, false);
    trans_d.fa = 26;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[1], trans, 1, false);
    trans_d.fa = 27;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[2], trans, 1, false);
    num_single = 1;
    num_range = 0;
    single = calloc(num_single, sizeof(char));
    assert(single != NULL);
    single[0] = '\'';
    min = calloc(num_range, sizeof(char));
    assert(min != NULL);
    max = calloc(num_range, sizeof(char));
    assert(max != NULL);
    trans_d.set = set_new(single, num_single, min, max, num_range);
    trans_init(&trans, TRANS_SET, trans_d);
    edge_init(&edges[3], trans, 2, true);
    state_init(&states[1], edges, num_edges, false);
    num_edges = 0;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    state_init(&states[2], edges, num_edges, true);
    num_edges = 4;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 30;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 3, false);
    trans_d.fa = 26;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[1], trans, 3, false);
    trans_d.fa = 28;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[2], trans, 3, false);
    num_single = 1;
    num_range = 0;
    single = calloc(num_single, sizeof(char));
    assert(single != NULL);
    single[0] = '"';
    min = calloc(num_range, sizeof(char));
    assert(min != NULL);
    max = calloc(num_range, sizeof(char));
    assert(max != NULL);
    trans_d.set = set_new(single, num_single, min, max, num_range);
    trans_init(&trans, TRANS_SET, trans_d);
    edge_init(&edges[3], trans, 2, true);
    state_init(&states[3], edges, num_edges, false);
    fa_init(&automata[25], MODE_LEFT, TYPE_REALSTRING, states, num_states);

    num_states = 4;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    num_single = 1;
    num_range = 0;
    single = calloc(num_single, sizeof(char));
    assert(single != NULL);
    single[0] = '\\';
    min = calloc(num_range, sizeof(char));
    assert(min != NULL);
    max = calloc(num_range, sizeof(char));
    assert(max != NULL);
    trans_d.set = set_new(single, num_single, min, max, num_range);
    trans_init(&trans, TRANS_SET, trans_d);
    edge_init(&edges[0], trans, 1, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 51;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 2, false);
    state_init(&states[1], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = '\0';
    trans_init(&trans, TRANS_WILD, trans_d);
    edge_init(&edges[0], trans, 3, false);
    state_init(&states[2], edges, num_edges, false);
    num_edges = 0;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    state_init(&states[3], edges, num_edges, true);
    fa_init(&automata[26], MODE_LEFT, TYPE_ESCAPED, states, num_states);

    num_states = 8;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 57;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 1, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 56;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 2, false);
    state_init(&states[1], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 55;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 3, false);
    state_init(&states[2], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = '\0';
    trans_init(&trans, TRANS_WILD, trans_d);
    edge_init(&edges[0], trans, 4, false);
    state_init(&states[3], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 54;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 5, false);
    state_init(&states[4], edges, num_edges, true);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 53;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 6, false);
    state_init(&states[5], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 52;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 7, false);
    state_init(&states[6], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = '\0';
    trans_init(&trans, TRANS_WILD, trans_d);
    edge_init(&edges[0], trans, 4, false);
    state_init(&states[7], edges, num_edges, false);
    fa_init(&automata[27], MODE_LEFT, TYPE_SINGLEQUOTELINE, states, num_states);

    num_states = 8;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 63;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 1, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 62;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 2, false);
    state_init(&states[1], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 61;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 3, false);
    state_init(&states[2], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = '\0';
    trans_init(&trans, TRANS_WILD, trans_d);
    edge_init(&edges[0], trans, 4, false);
    state_init(&states[3], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 60;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 5, false);
    state_init(&states[4], edges, num_edges, true);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 59;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 6, false);
    state_init(&states[5], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 58;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 7, false);
    state_init(&states[6], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = '\0';
    trans_init(&trans, TRANS_WILD, trans_d);
    edge_init(&edges[0], trans, 4, false);
    state_init(&states[7], edges, num_edges, false);
    fa_init(&automata[28], MODE_LEFT, TYPE_DOUBLEQUOTELINE, states, num_states);

    num_states = 14;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 2;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    num_single = 1;
    num_range = 0;
    single = calloc(num_single, sizeof(char));
    assert(single != NULL);
    single[0] = '\'';
    min = calloc(num_range, sizeof(char));
    assert(min != NULL);
    max = calloc(num_range, sizeof(char));
    assert(max != NULL);
    trans_d.set = set_new(single, num_single, min, max, num_range);
    trans_init(&trans, TRANS_SET, trans_d);
    edge_init(&edges[0], trans, 1, true);
    num_single = 1;
    num_range = 0;
    single = calloc(num_single, sizeof(char));
    assert(single != NULL);
    single[0] = '"';
    min = calloc(num_range, sizeof(char));
    assert(min != NULL);
    max = calloc(num_range, sizeof(char));
    assert(max != NULL);
    trans_d.set = set_new(single, num_single, min, max, num_range);
    trans_init(&trans, TRANS_SET, trans_d);
    edge_init(&edges[1], trans, 8, true);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 3;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    num_single = 1;
    num_range = 0;
    single = calloc(num_single, sizeof(char));
    assert(single != NULL);
    single[0] = '\\';
    min = calloc(num_range, sizeof(char));
    assert(min != NULL);
    max = calloc(num_range, sizeof(char));
    assert(max != NULL);
    trans_d.set = set_new(single, num_single, min, max, num_range);
    trans_init(&trans, TRANS_SET, trans_d);
    edge_init(&edges[0], trans, 2, false);
    trans_d.fa = 67;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[1], trans, 4, false);
    num_single = 1;
    num_range = 0;
    single = calloc(num_single, sizeof(char));
    assert(single != NULL);
    single[0] = '\'';
    min = calloc(num_range, sizeof(char));
    assert(min != NULL);
    max = calloc(num_range, sizeof(char));
    assert(max != NULL);
    trans_d.set = set_new(single, num_single, min, max, num_range);
    trans_init(&trans, TRANS_SET, trans_d);
    edge_init(&edges[2], trans, 7, true);
    state_init(&states[1], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 64;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 3, false);
    state_init(&states[2], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = '\0';
    trans_init(&trans, TRANS_WILD, trans_d);
    edge_init(&edges[0], trans, 1, false);
    state_init(&states[3], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 66;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 5, false);
    state_init(&states[4], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 65;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 6, false);
    state_init(&states[5], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = '\0';
    trans_init(&trans, TRANS_WILD, trans_d);
    edge_init(&edges[0], trans, 1, false);
    state_init(&states[6], edges, num_edges, false);
    num_edges = 0;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    state_init(&states[7], edges, num_edges, true);
    num_edges = 3;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    num_single = 1;
    num_range = 0;
    single = calloc(num_single, sizeof(char));
    assert(single != NULL);
    single[0] = '\\';
    min = calloc(num_range, sizeof(char));
    assert(min != NULL);
    max = calloc(num_range, sizeof(char));
    assert(max != NULL);
    trans_d.set = set_new(single, num_single, min, max, num_range);
    trans_init(&trans, TRANS_SET, trans_d);
    edge_init(&edges[0], trans, 9, false);
    trans_d.fa = 71;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[1], trans, 11, false);
    num_single = 1;
    num_range = 0;
    single = calloc(num_single, sizeof(char));
    assert(single != NULL);
    single[0] = '"';
    min = calloc(num_range, sizeof(char));
    assert(min != NULL);
    max = calloc(num_range, sizeof(char));
    assert(max != NULL);
    trans_d.set = set_new(single, num_single, min, max, num_range);
    trans_init(&trans, TRANS_SET, trans_d);
    edge_init(&edges[2], trans, 7, true);
    state_init(&states[8], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 68;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 10, false);
    state_init(&states[9], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = '\0';
    trans_init(&trans, TRANS_WILD, trans_d);
    edge_init(&edges[0], trans, 8, false);
    state_init(&states[10], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 70;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 12, false);
    state_init(&states[11], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 69;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 13, false);
    state_init(&states[12], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = '\0';
    trans_init(&trans, TRANS_WILD, trans_d);
    edge_init(&edges[0], trans, 8, false);
    state_init(&states[13], edges, num_edges, false);
    fa_init(&automata[29], MODE_LEFT, TYPE_REALSTRINGNONEWLINE, states, num_states);

    num_states = 3;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 3;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = '\r';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 1, true);
    trans_d.c = '\n';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[1], trans, 2, true);
    trans_d.c = '\r';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[2], trans, 2, true);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = '\n';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 2, true);
    state_init(&states[1], edges, num_edges, false);
    num_edges = 0;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    state_init(&states[2], edges, num_edges, true);
    fa_init(&automata[30], MODE_LEFT, TYPE_NEWLINE, states, num_states);

    num_states = 5;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = 'r';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 1, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = 'e';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 2, false);
    state_init(&states[1], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = 'p';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 3, false);
    state_init(&states[2], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = 'o';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 4, false);
    state_init(&states[3], edges, num_edges, false);
    num_edges = 0;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    state_init(&states[4], edges, num_edges, true);
    fa_init(&automata[31], MODE_VOID, TYPE_REPOTOKEN, states, num_states);

    num_states = 8;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = 'i';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 1, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = 'n';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 2, false);
    state_init(&states[1], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = 'c';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 3, false);
    state_init(&states[2], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = 'l';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 4, false);
    state_init(&states[3], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = 'u';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 5, false);
    state_init(&states[4], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = 'd';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 6, false);
    state_init(&states[5], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = 'e';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 7, false);
    state_init(&states[6], edges, num_edges, false);
    num_edges = 0;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    state_init(&states[7], edges, num_edges, true);
    fa_init(&automata[32], MODE_VOID, TYPE_INCLUDETOKEN, states, num_states);

    num_states = 4;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = 'a';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 1, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = 'd';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 2, false);
    state_init(&states[1], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = 'd';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 3, false);
    state_init(&states[2], edges, num_edges, false);
    num_edges = 0;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    state_init(&states[3], edges, num_edges, true);
    fa_init(&automata[33], MODE_VOID, TYPE_ADDTOKEN, states, num_states);

    num_states = 7;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = 'r';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 1, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = 'e';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 2, false);
    state_init(&states[1], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = 'm';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 3, false);
    state_init(&states[2], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = 'o';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 4, false);
    state_init(&states[3], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = 'v';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 5, false);
    state_init(&states[4], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = 'e';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 6, false);
    state_init(&states[5], edges, num_edges, false);
    num_edges = 0;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    state_init(&states[6], edges, num_edges, true);
    fa_init(&automata[34], MODE_VOID, TYPE_REMOVETOKEN, states, num_states);

    num_states = 5;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = 'm';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 1, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = 'o';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 2, false);
    state_init(&states[1], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = 'v';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 3, false);
    state_init(&states[2], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = 'e';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 4, false);
    state_init(&states[3], edges, num_edges, false);
    num_edges = 0;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    state_init(&states[4], edges, num_edges, true);
    fa_init(&automata[35], MODE_VOID, TYPE_MOVETOKEN, states, num_states);

    num_states = 4;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = 's';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 1, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = 'e';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 2, false);
    state_init(&states[1], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = 't';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 3, false);
    state_init(&states[2], edges, num_edges, false);
    num_edges = 0;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    state_init(&states[3], edges, num_edges, true);
    fa_init(&automata[36], MODE_VOID, TYPE_SETTOKEN, states, num_states);

    num_states = 7;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = 'a';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 1, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = 't';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 2, false);
    state_init(&states[1], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = 't';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 3, false);
    state_init(&states[2], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = 'a';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 4, false);
    state_init(&states[3], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = 'c';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 5, false);
    state_init(&states[4], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = 'h';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 6, false);
    state_init(&states[5], edges, num_edges, false);
    num_edges = 0;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    state_init(&states[6], edges, num_edges, true);
    fa_init(&automata[37], MODE_VOID, TYPE_ATTACHTOKEN, states, num_states);

    num_states = 7;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = 'd';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 1, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = 'e';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 2, false);
    state_init(&states[1], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = 't';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 3, false);
    state_init(&states[2], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = 'a';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 4, false);
    state_init(&states[3], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = 'c';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 5, false);
    state_init(&states[4], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = 'h';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 6, false);
    state_init(&states[5], edges, num_edges, false);
    num_edges = 0;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    state_init(&states[6], edges, num_edges, true);
    fa_init(&automata[38], MODE_VOID, TYPE_DETACHTOKEN, states, num_states);

    num_states = 8;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = 'n';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 1, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = 'e';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 2, false);
    state_init(&states[1], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = 't';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 3, false);
    state_init(&states[2], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = 'w';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 4, false);
    state_init(&states[3], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = 'o';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 5, false);
    state_init(&states[4], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = 'r';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 6, false);
    state_init(&states[5], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = 'k';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 7, false);
    state_init(&states[6], edges, num_edges, false);
    num_edges = 0;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    state_init(&states[7], edges, num_edges, true);
    fa_init(&automata[39], MODE_VOID, TYPE_NETWORKTOKEN, states, num_states);

    num_states = 5;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = 'b';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 1, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = 'i';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 2, false);
    state_init(&states[1], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = 'n';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 3, false);
    state_init(&states[2], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = 'd';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 4, false);
    state_init(&states[3], edges, num_edges, false);
    num_edges = 0;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    state_init(&states[4], edges, num_edges, true);
    fa_init(&automata[40], MODE_VOID, TYPE_BINDTOKEN, states, num_states);

    num_states = 7;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = 'u';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 1, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = 'n';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 2, false);
    state_init(&states[1], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = 'b';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 3, false);
    state_init(&states[2], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = 'i';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 4, false);
    state_init(&states[3], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = 'n';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 5, false);
    state_init(&states[4], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = 'd';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 6, false);
    state_init(&states[5], edges, num_edges, false);
    num_edges = 0;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    state_init(&states[6], edges, num_edges, true);
    fa_init(&automata[41], MODE_VOID, TYPE_UNBINDTOKEN, states, num_states);

    num_states = 10;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = 'n';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 1, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = 'a';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 2, false);
    state_init(&states[1], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = 'm';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 3, false);
    state_init(&states[2], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = 'e';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 4, false);
    state_init(&states[3], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = 's';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 5, false);
    state_init(&states[4], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = 'p';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 6, false);
    state_init(&states[5], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = 'a';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 7, false);
    state_init(&states[6], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = 'c';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 8, false);
    state_init(&states[7], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = 'e';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 9, false);
    state_init(&states[8], edges, num_edges, false);
    num_edges = 0;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    state_init(&states[9], edges, num_edges, true);
    fa_init(&automata[42], MODE_VOID, TYPE_NAMESPACETOKEN, states, num_states);

    num_states = 6;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = 's';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 1, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = 't';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 2, false);
    state_init(&states[1], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = 'a';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 3, false);
    state_init(&states[2], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = 'r';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 4, false);
    state_init(&states[3], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = 't';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 5, false);
    state_init(&states[4], edges, num_edges, false);
    num_edges = 0;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    state_init(&states[5], edges, num_edges, true);
    fa_init(&automata[43], MODE_VOID, TYPE_STARTTOKEN, states, num_states);

    num_states = 5;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = 's';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 1, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = 't';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 2, false);
    state_init(&states[1], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = 'o';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 3, false);
    state_init(&states[2], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = 'p';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 4, false);
    state_init(&states[3], edges, num_edges, false);
    num_edges = 0;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    state_init(&states[4], edges, num_edges, true);
    fa_init(&automata[44], MODE_VOID, TYPE_STOPTOKEN, states, num_states);

    num_states = 6;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = 'p';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 1, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = 'a';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 2, false);
    state_init(&states[1], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = 'u';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 3, false);
    state_init(&states[2], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = 's';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 4, false);
    state_init(&states[3], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = 'e';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 5, false);
    state_init(&states[4], edges, num_edges, false);
    num_edges = 0;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    state_init(&states[5], edges, num_edges, true);
    fa_init(&automata[45], MODE_VOID, TYPE_PAUSETOKEN, states, num_states);

    num_states = 4;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = '/';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 1, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = '/';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 2, false);
    state_init(&states[1], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 24;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 3, false);
    state_init(&states[2], edges, num_edges, true);
    num_edges = 0;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    state_init(&states[3], edges, num_edges, true);
    fa_init(&automata[46], MODE_VOID, TYPE_COMMENT, states, num_states);

    num_states = 3;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 3;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = '\r';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 1, false);
    trans_d.c = '\n';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[1], trans, 2, false);
    trans_d.c = '\r';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[2], trans, 2, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = '\n';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 2, false);
    state_init(&states[1], edges, num_edges, false);
    num_edges = 0;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    state_init(&states[2], edges, num_edges, true);
    fa_init(&automata[47], MODE_VOID, TYPE_EOL, states, num_states);

    num_states = 1;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 2;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    num_single = 2;
    num_range = 0;
    single = calloc(num_single, sizeof(char));
    assert(single != NULL);
    single[0] = '\t';
    single[1] = ' ';
    min = calloc(num_range, sizeof(char));
    assert(min != NULL);
    max = calloc(num_range, sizeof(char));
    assert(max != NULL);
    trans_d.set = set_new(single, num_single, min, max, num_range);
    trans_init(&trans, TRANS_SET, trans_d);
    edge_init(&edges[0], trans, 0, false);
    trans_d.fa = 47;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[1], trans, 0, false);
    state_init(&states[0], edges, num_edges, true);
    fa_init(&automata[48], MODE_VOID, TYPE_WS, states, num_states);

    num_states = 2;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 47;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 1, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 0;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    state_init(&states[1], edges, num_edges, true);
    fa_init(&automata[49], MODE_NEG, 0, states, num_states);

    num_states = 2;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 47;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 1, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 0;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    state_init(&states[1], edges, num_edges, true);
    fa_init(&automata[50], MODE_NEG, 0, states, num_states);

    num_states = 2;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 47;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 1, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 0;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    state_init(&states[1], edges, num_edges, true);
    fa_init(&automata[51], MODE_NEG, 0, states, num_states);

    num_states = 2;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 47;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 1, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 0;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    state_init(&states[1], edges, num_edges, true);
    fa_init(&automata[52], MODE_NEG, 0, states, num_states);

    num_states = 2;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    num_single = 1;
    num_range = 0;
    single = calloc(num_single, sizeof(char));
    assert(single != NULL);
    single[0] = '\\';
    min = calloc(num_range, sizeof(char));
    assert(min != NULL);
    max = calloc(num_range, sizeof(char));
    assert(max != NULL);
    trans_d.set = set_new(single, num_single, min, max, num_range);
    trans_init(&trans, TRANS_SET, trans_d);
    edge_init(&edges[0], trans, 1, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 0;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    state_init(&states[1], edges, num_edges, true);
    fa_init(&automata[53], MODE_NEG, 0, states, num_states);

    num_states = 2;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    num_single = 1;
    num_range = 0;
    single = calloc(num_single, sizeof(char));
    assert(single != NULL);
    single[0] = '\'';
    min = calloc(num_range, sizeof(char));
    assert(min != NULL);
    max = calloc(num_range, sizeof(char));
    assert(max != NULL);
    trans_d.set = set_new(single, num_single, min, max, num_range);
    trans_init(&trans, TRANS_SET, trans_d);
    edge_init(&edges[0], trans, 1, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 0;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    state_init(&states[1], edges, num_edges, true);
    fa_init(&automata[54], MODE_NEG, 0, states, num_states);

    num_states = 2;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 47;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 1, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 0;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    state_init(&states[1], edges, num_edges, true);
    fa_init(&automata[55], MODE_NEG, 0, states, num_states);

    num_states = 2;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    num_single = 1;
    num_range = 0;
    single = calloc(num_single, sizeof(char));
    assert(single != NULL);
    single[0] = '\\';
    min = calloc(num_range, sizeof(char));
    assert(min != NULL);
    max = calloc(num_range, sizeof(char));
    assert(max != NULL);
    trans_d.set = set_new(single, num_single, min, max, num_range);
    trans_init(&trans, TRANS_SET, trans_d);
    edge_init(&edges[0], trans, 1, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 0;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    state_init(&states[1], edges, num_edges, true);
    fa_init(&automata[56], MODE_NEG, 0, states, num_states);

    num_states = 2;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    num_single = 1;
    num_range = 0;
    single = calloc(num_single, sizeof(char));
    assert(single != NULL);
    single[0] = '\'';
    min = calloc(num_range, sizeof(char));
    assert(min != NULL);
    max = calloc(num_range, sizeof(char));
    assert(max != NULL);
    trans_d.set = set_new(single, num_single, min, max, num_range);
    trans_init(&trans, TRANS_SET, trans_d);
    edge_init(&edges[0], trans, 1, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 0;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    state_init(&states[1], edges, num_edges, true);
    fa_init(&automata[57], MODE_NEG, 0, states, num_states);

    num_states = 2;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 47;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 1, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 0;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    state_init(&states[1], edges, num_edges, true);
    fa_init(&automata[58], MODE_NEG, 0, states, num_states);

    num_states = 2;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    num_single = 1;
    num_range = 0;
    single = calloc(num_single, sizeof(char));
    assert(single != NULL);
    single[0] = '\\';
    min = calloc(num_range, sizeof(char));
    assert(min != NULL);
    max = calloc(num_range, sizeof(char));
    assert(max != NULL);
    trans_d.set = set_new(single, num_single, min, max, num_range);
    trans_init(&trans, TRANS_SET, trans_d);
    edge_init(&edges[0], trans, 1, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 0;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    state_init(&states[1], edges, num_edges, true);
    fa_init(&automata[59], MODE_NEG, 0, states, num_states);

    num_states = 2;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    num_single = 1;
    num_range = 0;
    single = calloc(num_single, sizeof(char));
    assert(single != NULL);
    single[0] = '"';
    min = calloc(num_range, sizeof(char));
    assert(min != NULL);
    max = calloc(num_range, sizeof(char));
    assert(max != NULL);
    trans_d.set = set_new(single, num_single, min, max, num_range);
    trans_init(&trans, TRANS_SET, trans_d);
    edge_init(&edges[0], trans, 1, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 0;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    state_init(&states[1], edges, num_edges, true);
    fa_init(&automata[60], MODE_NEG, 0, states, num_states);

    num_states = 2;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 47;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 1, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 0;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    state_init(&states[1], edges, num_edges, true);
    fa_init(&automata[61], MODE_NEG, 0, states, num_states);

    num_states = 2;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    num_single = 1;
    num_range = 0;
    single = calloc(num_single, sizeof(char));
    assert(single != NULL);
    single[0] = '\\';
    min = calloc(num_range, sizeof(char));
    assert(min != NULL);
    max = calloc(num_range, sizeof(char));
    assert(max != NULL);
    trans_d.set = set_new(single, num_single, min, max, num_range);
    trans_init(&trans, TRANS_SET, trans_d);
    edge_init(&edges[0], trans, 1, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 0;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    state_init(&states[1], edges, num_edges, true);
    fa_init(&automata[62], MODE_NEG, 0, states, num_states);

    num_states = 2;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    num_single = 1;
    num_range = 0;
    single = calloc(num_single, sizeof(char));
    assert(single != NULL);
    single[0] = '"';
    min = calloc(num_range, sizeof(char));
    assert(min != NULL);
    max = calloc(num_range, sizeof(char));
    assert(max != NULL);
    trans_d.set = set_new(single, num_single, min, max, num_range);
    trans_init(&trans, TRANS_SET, trans_d);
    edge_init(&edges[0], trans, 1, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 0;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    state_init(&states[1], edges, num_edges, true);
    fa_init(&automata[63], MODE_NEG, 0, states, num_states);

    num_states = 2;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 47;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 1, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 0;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    state_init(&states[1], edges, num_edges, true);
    fa_init(&automata[64], MODE_NEG, 0, states, num_states);

    num_states = 2;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 47;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 1, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 0;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    state_init(&states[1], edges, num_edges, true);
    fa_init(&automata[65], MODE_NEG, 0, states, num_states);

    num_states = 2;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    num_single = 1;
    num_range = 0;
    single = calloc(num_single, sizeof(char));
    assert(single != NULL);
    single[0] = '\\';
    min = calloc(num_range, sizeof(char));
    assert(min != NULL);
    max = calloc(num_range, sizeof(char));
    assert(max != NULL);
    trans_d.set = set_new(single, num_single, min, max, num_range);
    trans_init(&trans, TRANS_SET, trans_d);
    edge_init(&edges[0], trans, 1, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 0;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    state_init(&states[1], edges, num_edges, true);
    fa_init(&automata[66], MODE_NEG, 0, states, num_states);

    num_states = 2;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    num_single = 1;
    num_range = 0;
    single = calloc(num_single, sizeof(char));
    assert(single != NULL);
    single[0] = '\'';
    min = calloc(num_range, sizeof(char));
    assert(min != NULL);
    max = calloc(num_range, sizeof(char));
    assert(max != NULL);
    trans_d.set = set_new(single, num_single, min, max, num_range);
    trans_init(&trans, TRANS_SET, trans_d);
    edge_init(&edges[0], trans, 1, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 0;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    state_init(&states[1], edges, num_edges, true);
    fa_init(&automata[67], MODE_NEG, 0, states, num_states);

    num_states = 2;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 47;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 1, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 0;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    state_init(&states[1], edges, num_edges, true);
    fa_init(&automata[68], MODE_NEG, 0, states, num_states);

    num_states = 2;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 47;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 1, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 0;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    state_init(&states[1], edges, num_edges, true);
    fa_init(&automata[69], MODE_NEG, 0, states, num_states);

    num_states = 2;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    num_single = 1;
    num_range = 0;
    single = calloc(num_single, sizeof(char));
    assert(single != NULL);
    single[0] = '\\';
    min = calloc(num_range, sizeof(char));
    assert(min != NULL);
    max = calloc(num_range, sizeof(char));
    assert(max != NULL);
    trans_d.set = set_new(single, num_single, min, max, num_range);
    trans_init(&trans, TRANS_SET, trans_d);
    edge_init(&edges[0], trans, 1, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 0;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    state_init(&states[1], edges, num_edges, true);
    fa_init(&automata[70], MODE_NEG, 0, states, num_states);

    num_states = 2;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    num_single = 1;
    num_range = 0;
    single = calloc(num_single, sizeof(char));
    assert(single != NULL);
    single[0] = '"';
    min = calloc(num_range, sizeof(char));
    assert(min != NULL);
    max = calloc(num_range, sizeof(char));
    assert(max != NULL);
    trans_d.set = set_new(single, num_single, min, max, num_range);
    trans_init(&trans, TRANS_SET, trans_d);
    edge_init(&edges[0], trans, 1, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 0;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    state_init(&states[1], edges, num_edges, true);
    fa_init(&automata[71], MODE_NEG, 0, states, num_states);

    return wparser_new(start, automata, num_automata, eof_check);
}
